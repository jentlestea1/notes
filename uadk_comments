sched:

1. sec/hpre里把sched用起来？
4. sample调度器，poll_policy config入参没有必要

sec:
1. 重复代码多，需要重构
2. digest测试性能存在问题，需要 1. 解开算法层锁，2，分析smmu引入的问题。 -- 重点问题

hpre:

1. 使用HAVE_OPENSSL改造测试代码

zip:

1. zip_sva_test.c 里的测试case需要合入test_sva_perf

公共：

5. 一次编译出所有动态库和静态库
6. 交叉编译，编译成动态库(config.sh), 所有test app编译得到的是shell脚本
7. sec/hpre缺乏和SVA相关的测试用例(e.g. DVM, 预缺页, 大页...)
8. 所有算法层的异步msg pool都是为每个ctx都分配了，没有区分同步异步
9. comp, cipher的poll_ctx没有加锁
10. 全局变量中的g前缀去掉 g_xxx -> xxx
11. zip的测试app的返回值任何情况都是0，这样别的程序无法使用

下一步：

1. 性能测试，并发测试，长时间测试
2. 增加sched类型。配置参数搞成配置文件
...
